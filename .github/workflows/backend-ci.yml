name: Build Spring Boot Backend

on:
  push:
    branches: [ main ]
    paths:
      - 'Backend/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'Backend/**'

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: Backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for Maven wrapper
        run: chmod +x ./mvnw

      - name: Build Spring Boot app
        run: ./mvnw clean package -DskipTests

      - name: Build Docker image
        run: docker build -t journeygenie-backend .

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      - name: Tag Docker image as latest
        run: docker tag journeygenie-backend shaidurpranto/journeygenie-backend:latest

      - name: Push Docker image (latest only)
        run: docker push shaidurpranto/journeygenie-backend:latest
